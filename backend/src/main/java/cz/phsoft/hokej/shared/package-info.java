/**
 * Balíček shared obsahuje sdílené komponenty napříč celou aplikací.
 *
 * Jsou zde umístěny obecně použitelné třídy, které nejsou
 * vázány na konkrétní doménový modul, například:
 * - společná DTO pro standardizované odpovědi API,
 * - základní výjimky a chybové struktury,
 * - pomocné objekty využívané více vrstvami systému.
 *
 * Cílem balíčku je centralizovat opakovaně používané prvky,
 * minimalizovat duplicitu kódu a udržet konzistentní chování
 * napříč jednotlivými částmi aplikace.
 */
package cz.phsoft.hokej.shared;